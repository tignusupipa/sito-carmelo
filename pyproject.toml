[project]
name = "sito-carmelo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "google-auth",
    "google-api-python-client"
]

[tool.vercel]
entrypoint = "api.book:app"